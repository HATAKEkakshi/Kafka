version: '3.8'

services:
  kafka:
    build: .
    ports:
      - "2181:2181"
      - "9092:9092"
    environment:
      - KAFKA_HOST=${KAFKA_HOST}
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro